{
  "name": "cc-hub-relay",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "wrangler": "^4",
    "typescript": "^5",
    "@cloudflare/workers-types": "^4"
  }
}
